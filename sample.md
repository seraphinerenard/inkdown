# Welcome to Markdown Editor

This is a **native macOS** Markdown editor with *inline WYSIWYG* rendering.

## Features

- **Bold text** renders bold inline
- *Italic text* renders italic inline
- `inline code` renders in monospace
- Headings render at different sizes

### Code Blocks

```swift
struct ContentView: View {
    var body: some View {
        Text("Hello, world!")
    }
}
```

### Links and More

Visit [Apple Developer](https://developer.apple.com) for documentation.

> Block quotes are indented and styled differently.
> They can span multiple lines.

---

## Lists

1. First ordered item
2. Second ordered item
3. Third ordered item

- Unordered item one
- Unordered item two
- Unordered item three

### Nested Content

Here is some text with **bold**, *italic*, and ***bold italic*** together. You can also use `code spans` in the middle of a sentence.

~~Strikethrough text~~ is also supported.

That's it! Try clicking on different lines to see the syntax characters appear and disappear.
